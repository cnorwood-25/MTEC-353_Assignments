This week we looked at a lot of the fundamentals of C code and I learned a lot. One importnat thing we learned is the memory usage for each data type. Int is 4 bytes, float is 4 bytes, Double is 8 bytes, and char is 1 byte. There are other versions of declaring int that use more memory like long or long long, and a short which is only 2 bytes. We also learned the syntax for the different function we know in python or java (like if statements, for loops, declaring functions, and while loops), and applied them to C code. All things we learned were important for the final task of the midi note analyzer, and implementation was easier after the knowledge from class.
Some things that i learned working in the team was that in declaring functions, I do not need a semicolon and this creates an issue by separtating the statement. It will be important for me to remember that i should not declare functions inside of the main function. Additionally my teammate showed me that having two [] values next to an array makes it 2D with rows and columns.
I am excited to dive more into the rows and columns of the 2d array and see how this can augment data storage. i have a question about how to syntax it in the code, like what differiantes an entry from being in row 1 or 2? Also I am curious to learn more audio programming like in the labv exercise (how to generate and process audio)